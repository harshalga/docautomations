name: docautomations
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.2.1
  path_provider_android: ^2.2.15
  provider: ^6.1.0
  pdf: ^3.10.6
  printing: ^5.11.0
  open_file: ^3.3.2  # Use the latest version
  path_provider: ^2.1.2
  intl: ^0.18.0
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
   
flutter:
  uses-material-design: true

  assets:
    - images/
